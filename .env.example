# ─── Theme Configuration ───
# Set VITE_THEME to switch the application's brand and color palette.
# Supported values:
#   easydynamics  — Easy Dynamics brand (navy + orange)
#   oscalio       — OSCAL.io brand  (deep navy + cyan)
VITE_THEME=oscalio
